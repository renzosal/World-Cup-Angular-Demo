/*
describe('Controller: Dashboard', function () {

	var scope, ctrl;

	beforeEach(module('app'));
	beforeEach(inject(function($controller, $rootScope){
		scope = $rootScope.$new();

		ctr = $controller('dashboard as vm', { $scope: scope });
	}));


	it('should have matches defined', function () {
		console.log(scope);
		expect(1).toEqual(1);
	});
});
*/